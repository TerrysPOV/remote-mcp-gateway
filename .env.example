# Required by the gateway for connector auth
MCP_GATEWAY_API_KEY=change-me

# Optional: Comma-separated list of upstream MCP SSE URLs
UPSTREAM_URLS=

# Storage (default: local JSON/SQLite stubs)
DB_URL=
